node_modules/
.next/
next-env.d.ts
.env
.env*.local
.vercel/
playwright-report/
test-results/
# Coverage output. `npx vitest run --coverage.enabled=true` writes here.
# NOTE for anyone measuring coverage: the bare `--coverage` flag silently
# produces NO report in this setup — no directory, no table, exit 0. Use
# `--coverage.enabled=true`.
coverage/
coverage-qa/
.venv/
__pycache__/
.claude/settings.local.json
*.log
!CHANGELOG.log
*.tsbuildinfo
